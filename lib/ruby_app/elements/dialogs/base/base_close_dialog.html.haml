- self.content_for(:footer) do
  /
    #{__FILE__}
  = @close.render(:html) do |element|
    - element.content_for(:body, RubyApp::Elements::Dialogs::Base::BaseCloseDialog.translate.close.body)
= yield
