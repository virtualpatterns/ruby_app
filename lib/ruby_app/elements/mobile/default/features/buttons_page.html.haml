- self.content_for(:html, :page) do
  /
    #{__FILE__}
  %div{'data-role' => 'header'}
    = @back_button.render(:html)
    %h1
      Buttons
  %div{'data-role' => 'content'}
    %div{'data-role' => 'controlgroup'}
      = @button_link.render(:html) do |element|
        - element.content_for(:html, :link, 'Button')
